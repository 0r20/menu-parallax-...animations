import React from 'react'
// import styled from 'styled-components'

// const Styled = styled.div`
//     font-size: ${props => props.fz}px
// ` 

export const TextBlock = ({isAnimated, text}) => {
    return (
        <div>
            {text}
        </div>
    )
}
